# --- ОС и редакторы кода (самое важное) ---

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# VS Code (настройки рабочего пространства конкретного пользователя)
.vscode/settings.json
.vscode/tasks.json
.vscode/launch.json
.vscode/extensions.json
# Но можно оставить .vscode/code-workspace, если используете workspace

# Другие IDE
.idea/
*.swp
*.swo
*~

# --- Секреты и окружение (КРИТИЧНО) ---

# Если вдруг начнешь использовать переменные окружения
.env
.env.local
.env.production

# --- Временные файлы и логи ---

# Логи npm/yarn (если вдруг установишь пакетный менеджер)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Папка зависимостей (на будущее, если подключишь npm)
node_modules/

# --- Сборщики и инструменты (на будущее) ---

# Если будешь использовать Vite, Webpack, Parcel, Gulp и т.д.
dist/
build/
coverage/